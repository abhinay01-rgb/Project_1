def hybrid_retriever(query):
    return [
        {"content": "Neural networks are computational models inspired by biological neurons."},
        {"content": "Backpropagation is used to train neural networks by minimizing loss."}
    ]